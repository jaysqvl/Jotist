# Jotist. Legacy service and volume names preserve existing deployments.
# Docker Compose for Jotist with Blackwell GPU support (RTX 50-series)
# Uses pre-built image from GitHub Container Registry
# For legacy GPUs (GTX 10-series through RTX 40-series), use docker-compose.cuda.yml instead
version: "3.9"
services:
  scriberr:
    image: ghcr.io/jaysqvl/jotist:1.7.0-rc.1-blackwell
    ports:
      - "8080:8080"
    volumes:
      - scriberr_data:/app/data
      - env_data:/app/whisperx-env
    restart: unless-stopped
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities:
                - gpu
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
      - NVIDIA_DRIVER_CAPABILITIES=compute,utility
      - PUID=${PUID:-1000}
      - PGID=${PGID:-1000}
      # Security: already set in container, but can be overridden
      - APP_ENV=production
      # CORS: comma-separated list of allowed origins for production
      # - ALLOWED_ORIGINS=https://your-domain.com

volumes:
  scriberr_data: {}
  env_data: {}
# Legacy RC1 image: updated CUDA 13 source builds await Blackwell inference qualification.
