# Jotist. Legacy service and volume names preserve existing deployments.
version: "3.9"

services:
  scriberr:
    # Build from local Dockerfile; replace with `image: ghcr.io/jaysqvl/jotist:1.7.0`
    # if you prefer pulling a prebuilt image.
    build:
      context: .
      dockerfile: Dockerfile
    image: jotist:local
    # image: ghcr.io/jaysqvl/jotist:1.7.0
    container_name: scriberr
    ports:
      - "8080:8080"
    # environment:
    #   - HOST=0.0.0.0
    #   - PORT=8080
    #   - DATABASE_PATH=/app/data/scriberr.db
    #   - UPLOAD_DIR=/app/data/uploads
    #   - PUID=${PUID:-1000}
    #   - PGID=${PGID:-1000}
    environment:
      # Security: already set in container, but can be overridden
      - APP_ENV=production
      # CORS: comma-separated list of allowed origins for production
      # - ALLOWED_ORIGINS=https://your-domain.com
    volumes:
      - ./scriberr-data:/app/data
      - ./env-data:/app/whisperx-env
    restart: unless-stopped

volumes:
  scriberr-data:
